### **Key ECMAScript Features by Version**

ECMAScript (ES) is the standard behind JavaScript. Each version introduces new features. Here are the most important ones:

---

### **🔹 ES6 (2015) – Major Update**

✅ **`let` & `const`** (Block-scoped variables)  
✅ **Arrow functions (`\=>`)**  
✅ **Template literals ( )** – `${variable}` inside strings  
✅ **Destructuring** – Extract values from arrays/objects  
✅ **Spread (`...`) & Rest parameters (`...`)**  
✅ **Default function parameters**  
✅ **Promises** – Handle async operations  
✅ **Classes (`class`)** – Object-oriented programming  
✅ **Modules (`import/export`)**

---

### **🔹 ES7 (2016)**

✅ **Exponentiation (`**`)** – `2 ** 3` → `8`  
✅ **`Array.prototype.includes()`** – `arr.includes(value)`

---

### **🔹 ES8 (2017)**

✅ **`async/await`** – Cleaner async code  
✅ **Object.entries() & Object.values()**  
✅ **String padding (`padStart`, `padEnd`)**

---

### **🔹 ES9 (2018)**

✅ **Rest/Spread for Objects** – `{ ...obj }`  
✅ **Promise.prototype.finally()**

---

### **🔹 ES10 (2019)**

✅ **`Array.flat()` & `Array.flatMap()`**  
✅ **Optional catch binding (`catch {}`)**

---

### **🔹 ES11 (2020)**

✅ **Optional Chaining (`?.`)** – `obj?.prop`  
✅ **Nullish Coalescing (`??`)** – `null ?? "default"`  
✅ **Dynamic Imports (`import()`)**

---

### **🔹 ES12 (2021) – Latest Stable**

✅ **Numeric Separators (`1_000_000`)**  
✅ **Logical Assignment (`||=`, `&&=`, `??=`)**  
✅ **String `.replaceAll()`**

---

### **🔹 Latest Features (2022-2024)**

✅ **`Array.at()`** – Easier negative indexing  
✅ **Top-level `await`** in modules  
✅ **`Array.findLast()` & `Array.findLastIndex()`**

---

### **🔹 Key Takeaways**

✔ **ES6 introduced major changes** (let, const, classes, modules)  
✔ **Async improvements** (`async/await`, Promises)  
✔ **Better array/object manipulation** (`flat()`, spread, destructuring)

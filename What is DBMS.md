**DBMS** stands for **Database Management System**. It is software that allows users to **create**, **store**, **manage**, **retrieve**, and **update** data in **databases** efficiently and securely.

### 🔹 In Simple Terms:

A **DBMS** acts like a **middleman** between the user and the database. You don't need to manually manage or organize all the data files — the DBMS does that for you.

### 🔧 Main Functions of a DBMS:

1. **Data Storage Management** – Storing data in a structured way.
2. **Data Retrieval** – Querying data using languages like SQL.
3. **Data Manipulation** – Inserting, updating, deleting data.
4. **Data Security** – Controlling who can access what.
5. **Data Integrity** – Ensuring data is accurate and consistent.
6. **Backup and Recovery** – Preventing data loss during failures.

### 📊 Examples of DBMS:

- **MySQL**
- **PostgreSQL**
- **Oracle Database**
- **Microsoft SQL Server**
- **MongoDB** (NoSQL type)    

### 📁 Types of DBMS:

1. **Relational DBMS (RDBMS)** – Uses tables (e.g., MySQL, PostgreSQL)
2. **NoSQL DBMS** – Uses documents, key-values, etc. (e.g., MongoDB)
3. **Hierarchical DBMS** – Tree-like structure   
4. **Network DBMS** – Complex relationships using graphs
